import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    icon: {
        color: 'white',
        fontSize: 20
    },
    gift: {

    },
    headerText: {
        color: 'white',
        fontSize: 16,
        fontWeight: '800'
    }
});

export default styles;